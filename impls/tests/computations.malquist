;; Compute a Fibonacci number with two recursions.
(def! sumdown
  (fn* [n]
    (if (= n 0)
      0
      (+ n (sumdown  (- n 1))))))

(def! fib
  (fn* [n]
    (if (<= n 1)
      n
      (+ (fib (- n 1)) (fib (- n 2))))))
